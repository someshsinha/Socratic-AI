// Shared auth constants — kept in a standalone file to avoid circular imports
// between App.jsx (which imports Navbar) and Navbar.jsx (which needs REDIRECT_KEY)
export const REDIRECT_KEY = 'auth_redirect_to';
